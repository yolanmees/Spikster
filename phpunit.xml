<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         beStrictAboutOutputDuringTests="true"
         failOnRisky="true"
         failOnWarning="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>

        <testsuite name="Feature">
            <directory>tests/Feature</directory>
            <exclude>tests/Feature/Livewire</exclude>
        </testsuite>

        <testsuite name="Livewire">
            <directory>tests/Feature/Livewire</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory>app</directory>
        </include>
        <exclude>
            <directory>app/Console/Commands</directory>
            <file>app/Exceptions/Handler.php</file>
            <file>app/Http/Kernel.php</file>
            <file>app/Console/Kernel.php</file>
        </exclude>
    </source>

    <php>
        <!-- Application -->
        <env name="APP_ENV" value="testing"/>
        <env name="APP_KEY" value="base64:2fl3ePxVfAJqH1CnvN3zQT2bDrM8cR5yP0zLvK9wXmA="/>
        <env name="APP_DEBUG" value="true"/>
        <env name="APP_URL" value="http://localhost"/>

        <!-- Database -->
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>

        <!-- Cache & Sessions -->
        <env name="CACHE_DRIVER" value="array"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>

        <!-- Mail -->
        <env name="MAIL_MAILER" value="array"/>

        <!-- Security -->
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="HASH_DRIVER" value="bcrypt"/>

        <!-- Telescope & Debugging -->
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="LOG_CHANNEL" value="null"/>

        <!-- Redis (if needed) -->
        <env name="REDIS_CLIENT" value="phpredis"/>

        <!-- Livewire -->
        <env name="LIVEWIRE_ASSET_URL" value="http://localhost"/>
    </php>
</phpunit>
